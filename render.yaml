startCommand: gunicorn portfolio-en-Django.wsgi


# render.yaml

name: portfolio-en-Django
env:
  - key: PYTHON_VERSION
    value: "3.11.3"
  - key: SECRET_KEY
    value: "Vicente22@"

startCommand: gunicorn portfolio-en-Django.wsgi

routes:
  - path: /
    command: start
  - path: /admin
    command: python manage.py runserver

env:
  - key: DEBUG
    value: "false"

ports:
  - port: 8000
    protocol: http
